
@{
    ViewBag.Title = "ListDocument";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<main>
    @{ var userInfomatiom = (ToeicAspMVC.Models.User)Session["User"]; }
    <!--archive header-->
    <div class="archive-header pt-50">
        <div class="container">
            <h2 class="font-weight-900">Danh sách tài liệu</h2>

            <div class="bt-1 border-color-1 mt-30 mb-50"></div>
        </div>
    </div>
    <div class="pb-50">
        <div class="container">
            <div class="row">
                <div class="col-lg-8">
                    <div class="post-module-3">
                        <div class="loop-list loop-list-style-1">
                            @foreach (var item in ViewBag.List)
                            {
                                <article class="hover-up-2 transition-normal wow fadeInUp animated">
                                    <div class="row mb-40 list-style-2">

                                        <div class="col-md-12 align-self-center">
                                            <div class="post-content">
                                                <h5 class="post-title font-weight-900 mb-20">
                                                    <a href="/Document/Detail/@item.idDocument">@item.name</a>
                                                    <span class="post-format-icon"><i class="elegant-icon icon_star_alt"></i></span>
                                                </h5>
                                                <div class="post-excerpt mb-25 font-small text-muted">
                                                    <div class="a" style="white-space: nowrap; width: 600px; height:30px; overflow: hidden; text-overflow: ellipsis;">@Html.Raw(item.content)</div>
                                                </div>
                                                <div class="entry-meta meta-1 float-left font-x-small text-uppercase">
                                                    <span class="post-on">@item.created</span>
                                                    @if (userInfomatiom != null)
                                                    {
                                                        if (item.User.idUser == userInfomatiom.idUser)
                                                        {
                                                           
                                                            if (item.status == 0)
                                                            {
                                                                <button type="button" class="btn btn-danger btn-sm ">Đang chờ duyệt</button>
                                                            }
                                                            else if (item.status == 1)
                                                            {
                                                                <button type="button" class="btn btn-success btn-sm ">Đã duyệt</button>
                                                            }
                                                            else
                                                            {
                                                                <button type="button" class="btn btn-warning btn-sm ">Bị chặn</button>
                                                            }

                                                        }
                                                        else
                                                        {
                                                            <span></span>
                                                        }
                                                    }
                                                    else
                                                    {
                                                        <span></span>
                                                    }
                                                </div>

                                            </div>
                                        </div>
                                    </div>
                                </article>
                            }

                        </div>
                    </div>
                    <div class="pagination-area mb-30 wow fadeInUp animated">
                        <nav aria-label="Page navigation example">
                            <ul class="pagination justify-content-start">
                                @if (ViewBag.tag > 1)
                                {
                                    int b = ViewBag.tag - 1;
                                    <li class="page-item"><a class="page-link" href="/Document/ListDocument/@ViewBag.User.idUser/@b"><i class="elegant-icon arrow_left"></i></a></li>
                                }
                                else
                                {
                                    <li></li>
                                }

                                @for (var i = 1; i <= ViewBag.pageSize; i++)
                                {
                                    if (ViewBag.tag == i)
                                    {
                                        <li class="page-item active"><a class="page-link" href="/Document/ListDocument/@ViewBag.User.idUser/@i">@i</a></li>
                                    }
                                    else
                                    {
                                        <li class="page-item "><a class="page-link" href="/Document/ListDocument/@ViewBag.User.idUser/@i">@i</a></li>
                                    }

                                }
                                @if (ViewBag.tag < ViewBag.pageSize)
                                {
                                    int a = ViewBag.tag + 1;
                                    <li class="page-item"><a class="page-link" href="/Document/ListDocument/@ViewBag.User.idUser/@a"><i class="elegant-icon arrow_right"></i></a></li>
                                }
                                else
                                {
                                    <li></li>
                                }

                            </ul>
                        </nav>
                    </div>
                </div>
                @{ var user = ViewBag.User;}
                <div class="col-lg-4">
                    <div class="widget-area">
                        <div class="sidebar-widget widget-about mb-50 pt-30 pr-30 pb-30 pl-30 bg-white border-radius-5 has-border  wow fadeInUp animated">
                            <img class="about-author-img mb-25" src="/Content/assets/imgs/authors/author.jpg" alt="">
                            <h5 class="mb-20">Xin chào, Tôi là @user.fullName</h5>

                            <strong>Theo dõi tôi: </strong>
                            <ul class="header-social-network d-inline-block list-inline color-white mb-20">
                                <li class="list-inline-item"><a class="fb" href="#" target="_blank" title="Facebook"><i class="elegant-icon social_facebook"></i></a></li>
                                <li class="list-inline-item"><a class="tw" href="#" target="_blank" title="Tweet now"><i class="elegant-icon social_twitter"></i></a></li>
                                <li class="list-inline-item"><a class="pt" href="#" target="_blank" title="Pin it"><i class="elegant-icon social_pinterest"></i></a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</main>


